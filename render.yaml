services:
  - type: web
    name: notifications-service
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: prod
      - key: MCP_TRANSPORT
        value: http
      - key: EMAIL_API_KEY
        sync: false
      - key: GOOGLE_APPLICATION_CREDENTIALS_JSON
        sync: false
